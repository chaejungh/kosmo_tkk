spring:
  config:
    activate:
      on-profile: deploy

  datasource:
    # 🔥 Docker Oracle XE가 host 3000 포트로 열려있던 예전 설정으로 복귀
    url: jdbc:mysql://thekuku.czmmuc4g8era.ap-northeast-2.rds.amazonaws.com:3306/tkk
    username: thekuku
    password: thekukupw
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
        use_sql_comments: true
        dialect: org.hibernate.dialect.OracleDialect
        highlight_sql: true
logging:
  level:
    org.hibernate.orm.jdbc.bind: trace

server:
  port: 443
  ssl:
    enabled: true
    key-store: classpath:ssl/thekuku-https.p12
    key-store-password: thekuku123!    # keytool에서 입력한 비번
    key-store-type: PKCS12